---
phase: 03-instagram-public-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intake/types.ts
  - src/intake/policy.ts
  - src/intake/resolver.ts
  - src/intake/adapters/instagram.ts
  - src/intake/tools/yt-dlp.ts
  - test/intake/instagram-adapter.test.ts
autonomous: true
requirements: [SRC-02]
user_setup: []
must_haves:
  truths:
    - "Instagram public post/reel/video URLs are classified through a dedicated intake path"
    - "Supported public Instagram URLs resolve to a normalized intake source contract"
    - "Instagram intake is isolated behind adapter/tool boundaries and does not change existing source contracts"
  artifacts:
    - "src/intake/policy.ts"
    - "src/intake/adapters/instagram.ts"
    - "src/intake/resolver.ts"
    - "test/intake/instagram-adapter.test.ts"
  key_links:
    - "`src/intake/resolver.ts` routes instagram classification to `resolveInstagramSource()`"
    - "`src/intake/adapters/instagram.ts` delegates extraction to `src/intake/tools/yt-dlp.ts`"
---

<objective>
Implement Instagram public URL intake foundation via existing extractor boundary.

Purpose: Satisfy the core SRC-02 success path using contract-first resolver/adapters.
Output: Instagram URL classification, adapter wiring, and deterministic success-path tests.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-instagram-public-intake/03-RESEARCH.md
@src/intake/types.ts
@src/intake/policy.ts
@src/intake/resolver.ts
@src/intake/tools/yt-dlp.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend intake contracts and policy for Instagram public URL classification</name>
  <files>src/intake/types.ts, src/intake/policy.ts, src/intake/resolver.ts</files>
  <action>Add Instagram classification support for public post/reel/video URL forms (`instagram.com` hosts with `/p/`, `/reel/`, `/tv/` paths). Update source classification/type contracts and resolver dispatch to include an Instagram branch while preserving existing behaviors for YouTube/direct/local inputs.</action>
  <verify><automated>bun test test/intake/source-resolver.test.ts</automated></verify>
  <done>Resolver deterministically classifies supported Instagram public URLs and routes to Instagram adapter boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Instagram adapter over extractor boundary</name>
  <files>src/intake/adapters/instagram.ts, src/intake/tools/yt-dlp.ts</files>
  <action>Create `resolveInstagramSource(input)` that validates Instagram URL shape, invokes extractor tooling, and returns normalized contract fields (kind/originalInput/normalizedUrl/mediaUrl plus optional metadata fields if available). Reuse existing parser utilities where practical instead of introducing a second extraction stack.</action>
  <verify><automated>bun test test/intake/instagram-adapter.test.ts</automated></verify>
  <done>Instagram adapter resolves supported public URLs and returns contract-compliant source data.</done>
</task>

<task type="auto">
  <name>Task 3: Add Instagram adapter success-path regression tests</name>
  <files>test/intake/instagram-adapter.test.ts</files>
  <action>Add unit tests for valid public Instagram URLs that assert deterministic contract shape and extractor boundary integration. Include invalid Instagram URL-shape rejection coverage with stable error-code assertions.</action>
  <verify><automated>bun test test/intake/instagram-adapter.test.ts</automated></verify>
  <done>Instagram adapter tests pass and prevent regressions in public URL intake behavior.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/intake/instagram-adapter.test.ts` passes
- [ ] Resolver routes supported Instagram public URLs to dedicated adapter
- [ ] Existing YouTube/direct/local paths remain unchanged and green
</verification>

<success_criteria>

- All tasks completed
- SRC-02 core Instagram public intake path implemented behind stable adapter boundaries
- Instagram success-path behavior has deterministic automated coverage

</success_criteria>

<output>
After completion, create `.planning/phases/03-instagram-public-intake/03-01-SUMMARY.md`
</output>
