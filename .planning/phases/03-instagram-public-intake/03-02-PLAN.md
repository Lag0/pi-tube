---
phase: 03-instagram-public-intake
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/errors/cli-errors.ts
  - src/intake/adapters/instagram.ts
  - src/intake/tools/yt-dlp.ts
  - src/cli/handlers.ts
  - test/intake/instagram-adapter.test.ts
  - test/cli/intake-cli.test.ts
autonomous: true
requirements: [SRC-03]
user_setup: []
must_haves:
  truths:
    - "Instagram URLs that require authentication fail with `INSTAGRAM_AUTH_REQUIRED`"
    - "Auth-required Instagram failures produce non-zero CLI exit status with remediation guidance"
    - "Instagram extraction failures that are not auth-related stay mapped to a separate stable error code"
  artifacts:
    - "src/errors/cli-errors.ts"
    - "src/intake/tools/yt-dlp.ts"
    - "src/intake/adapters/instagram.ts"
    - "test/cli/intake-cli.test.ts"
  key_links:
    - "`src/intake/tools/yt-dlp.ts` maps extractor auth signatures to `INSTAGRAM_AUTH_REQUIRED`"
    - "`src/cli/handlers.ts` and error formatter surface remediation guidance for auth-required cases"
---

<objective>
Implement explicit auth-required detection and stable Instagram error mapping.

Purpose: Satisfy SRC-03 with deterministic code + guidance semantics at both adapter and CLI boundaries.
Output: Auth-required detection logic, new stable error constructors, and smoke coverage for CLI behavior.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-instagram-public-intake/03-RESEARCH.md
@.planning/phases/03-instagram-public-intake/03-01-PLAN.md
@src/errors/cli-errors.ts
@src/intake/tools/yt-dlp.ts
@src/intake/adapters/instagram.ts
@src/cli/handlers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stable Instagram auth-required and extraction-failure error constructors</name>
  <files>src/errors/cli-errors.ts</files>
  <action>Create explicit error helpers for Instagram auth-required and generic Instagram extraction failures. Ensure guidance text explains public-only policy and remediation (use public URL or remove auth-gated content).</action>
  <verify><automated>bun test test/intake/instagram-adapter.test.ts</automated></verify>
  <done>New Instagram error constructors produce deterministic code/exit/guidance contract for auth and non-auth failures.</done>
</task>

<task type="auto">
  <name>Task 2: Detect auth-required extractor failures and map to `INSTAGRAM_AUTH_REQUIRED`</name>
  <files>src/intake/tools/yt-dlp.ts, src/intake/adapters/instagram.ts</files>
  <action>Add extractor failure classification for known authentication-required signatures (login/challenge/private-auth markers) and map them to the new auth-required error code. Preserve a separate deterministic fallback for non-auth Instagram extraction failures.</action>
  <verify><automated>bun test test/intake/instagram-adapter.test.ts</automated></verify>
  <done>Instagram auth-required scenarios are reliably identified and mapped to `INSTAGRAM_AUTH_REQUIRED`.</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI-facing auth-required smoke coverage</name>
  <files>test/cli/intake-cli.test.ts, test/intake/instagram-adapter.test.ts, src/cli/handlers.ts</files>
  <action>Add tests verifying baseline `pi-tube <instagram-url>` exits non-zero and includes remediation guidance when auth is required. Confirm code + guidance are asserted by tokenized expectations (not brittle full-text snapshots).</action>
  <verify><automated>bun test test/intake/instagram-adapter.test.ts test/cli/intake-cli.test.ts</automated></verify>
  <done>CLI contract for auth-required Instagram failures is deterministic and regression-tested.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/intake/instagram-adapter.test.ts test/cli/intake-cli.test.ts` passes
- [ ] Auth-required Instagram failures map to `INSTAGRAM_AUTH_REQUIRED`
- [ ] CLI exits non-zero and includes remediation guidance for auth-required cases
</verification>

<success_criteria>

- All tasks completed
- SRC-03 behavior is deterministic and user-visible through CLI output semantics
- Auth-required mapping is isolated, test-covered, and not conflated with generic extractor failure

</success_criteria>

<output>
After completion, create `.planning/phases/03-instagram-public-intake/03-02-SUMMARY.md`
</output>
