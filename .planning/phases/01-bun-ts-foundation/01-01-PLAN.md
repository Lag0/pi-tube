---
phase: 01-bun-ts-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - bin/pi-tube.ts
  - src/index.ts
  - src/cli/build-cli.ts
  - src/cli/command-contract.ts
  - src/cli/handlers.ts
  - test/cli/help.test.ts
  - test/cli/entrypoint.test.ts
autonomous: true
requirements: [MIGR-01, MIGR-02]
user_setup: []
must_haves:
  truths:
    - "`pi-tube` has a Bun/TS executable entrypoint that runs locally"
    - "Core CLI contract primitives are centralized in TypeScript"
  artifacts:
    - "bin/pi-tube.ts"
    - "src/cli/command-contract.ts"
    - "src/cli/build-cli.ts"
  key_links:
    - "bin/pi-tube.ts bootstraps src/index.ts"
    - "src/index.ts calls CLI builder from src/cli/build-cli.ts"
---

<objective>
Bootstrap Bun + TypeScript project structure and establish the canonical `pi-tube` executable entrypoint.

Purpose: Replace Python-first runtime entry with Bun/TS while preserving command identity.
Output: Working TS/Bun CLI scaffold with test scaffolding for entry/help contracts.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bun-ts-foundation/01-CONTEXT.md
@.planning/phases/01-bun-ts-foundation/01-RESEARCH.md
@pyproject.toml
@pi_tube/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Bun + TypeScript CLI runtime</name>
  <files>package.json, tsconfig.json, src/index.ts, bin/pi-tube.ts</files>
  <action>Create the Bun/TS project baseline for the CLI. Add package metadata that keeps the public command identity as `pi-tube`, create TypeScript compiler config, and add a Bun-executable bin entry (`#!/usr/bin/env bun`) that calls `src/index.ts`. Keep this plan focused on runtime/entry bootstrap only.</action>
  <verify><automated>bun run --bun bin/pi-tube.ts --help</automated></verify>
  <done>Running the Bun entrypoint prints CLI help output and exits successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Define contract-first CLI modules</name>
  <files>src/cli/command-contract.ts, src/cli/build-cli.ts, src/cli/handlers.ts, src/index.ts</files>
  <action>Implement a contract module for locked global flags (`--help`, `--version`, `--json`) and baseline positional input semantics (`pi-tube <input>`). Wire a minimal command builder that routes to deterministic baseline handlers and preserves room for later phase source/provider expansion.</action>
  <verify><automated>bun run --bun bin/pi-tube.ts sample-input</automated></verify>
  <done>CLI builder is wired and baseline positional input path executes a deterministic handler.</done>
</task>

<task type="auto">
  <name>Task 3: Add Wave 0 test scaffolding for migration contracts</name>
  <files>test/cli/help.test.ts, test/cli/entrypoint.test.ts</files>
  <action>Create initial Bun test files for help structure and entrypoint behavior with explicit TODO-marked assertions for final behavior added in later plans. Ensure test files execute in Bun test runner and become the base for Nyquist automated verification continuity.</action>
  <verify><automated>bun test test/cli/help.test.ts test/cli/entrypoint.test.ts</automated></verify>
  <done>Test files exist, are discoverable by Bun test, and run without framework/setup errors.</done>
</task>

</tasks>

<verification>
- [ ] `bun run --bun bin/pi-tube.ts --help` executes from repo root
- [ ] `bun test test/cli/help.test.ts test/cli/entrypoint.test.ts` passes
- [ ] Command identity in scaffolded metadata remains `pi-tube`
</verification>

<success_criteria>

- All tasks completed
- Bun/TS entrypoint exists and runs
- Contract-first CLI modules are established
- Wave 0 tests exist for help + entrypoint

</success_criteria>

<output>
After completion, create `.planning/phases/01-bun-ts-foundation/01-01-SUMMARY.md`
</output>
