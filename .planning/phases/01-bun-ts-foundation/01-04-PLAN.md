---
phase: 01-bun-ts-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - README.md
  - install.sh
  - test/cli/install-flow.test.ts
  - test/cli/help.test.ts
autonomous: true
requirements: [MIGR-01, MIGR-03, CLI-01]
user_setup: []
must_haves:
  truths:
    - "A documented install/run flow yields a working `pi-tube` command on macOS/Linux"
    - "Top-level docs match the actual Bun/TS command behavior and help output"
    - "Phase 1 roadmap plan items are objectively verifiable via tests/checks"
  artifacts:
    - "README.md"
    - "install.sh"
    - "test/cli/install-flow.test.ts"
  key_links:
    - "Install script and README command examples both target canonical `pi-tube` invocation"
    - "Install-flow test validates docs instructions against real CLI behavior"
---

<objective>
Validate install/run flow and update top-level CLI usage docs for the Bun/TS foundation.

Purpose: Close the migration loop by aligning docs, installer behavior, and executable contract.
Output: Verified install guidance and final documentation updates proving Phase 1 usability.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bun-ts-foundation/01-CONTEXT.md
@.planning/phases/01-bun-ts-foundation/01-RESEARCH.md
@README.md
@install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add install/run contract verification test</name>
  <files>test/cli/install-flow.test.ts, test/cli/help.test.ts</files>
  <action>Create a test that validates the documented install/run command path assumptions, including canonical invocation (`pi-tube --help`) and required help sections. Keep checks deterministic and fast so they can run as phase regression gate before documentation updates ship.</action>
  <verify><automated>bun test test/cli/install-flow.test.ts test/cli/help.test.ts</automated></verify>
  <done>Install/run contract tests pass and fail clearly when docs/behavior drift.</done>
</task>

<task type="auto">
  <name>Task 2: Update README and installer guidance to Bun/TS primary path</name>
  <files>README.md, install.sh</files>
  <action>Revise top-level usage docs and install flow to prioritize canonical `pi-tube` command on Bun/TS, keeping contributor/dev notes separate from user-facing instructions. Ensure language is concise and operational, and examples are copy-paste ready. Preserve phase-accurate labeling for features not yet implemented.</action>
  <verify><automated>rg -n "pi-tube --help|coming soon|Bun|TypeScript" README.md install.sh</automated></verify>
  <done>README and installer messaging match locked context decisions and current Phase 1 capabilities.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/cli/install-flow.test.ts test/cli/help.test.ts` passes
- [ ] README/install flow commands reflect canonical `pi-tube` path
</verification>

<success_criteria>

- All tasks completed
- Documented install/run path is validated against CLI behavior
- User-facing docs are Bun/TS-first and operational

</success_criteria>

<output>
After completion, create `.planning/phases/01-bun-ts-foundation/01-04-SUMMARY.md`
</output>
