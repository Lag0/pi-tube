---
phase: 01-bun-ts-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/legacy/compatibility.ts
  - src/cli/handlers.ts
  - src/errors/cli-errors.ts
  - package.json
  - src/index.ts
  - README.md
  - test/cli/no-python-runtime.test.ts
autonomous: true
requirements: [MIGR-03, MIGR-01]
user_setup: []
must_haves:
  truths:
    - "Primary runtime path executes without Python runtime dependency"
    - "Legacy invocation patterns receive deterministic compatibility guidance"
    - "Default user path remains Bun/TS and keeps `pi-tube` identity"
  artifacts:
    - "src/legacy/compatibility.ts"
    - "test/cli/no-python-runtime.test.ts"
    - "package.json"
  key_links:
    - "CLI entrypoint routes legacy patterns through compatibility handler"
    - "No-Python test validates primary path behavior"
---

<objective>
Wire legacy compatibility shim and remove Python runtime dependency from the main execution path.

Purpose: Complete migration cutover for default CLI path while preserving user guidance and compatibility messaging.
Output: Compatibility handler, updated runtime scripts/metadata, and tests proving Python-free primary execution.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bun-ts-foundation/01-CONTEXT.md
@.planning/phases/01-bun-ts-foundation/01-RESEARCH.md
@pi_tube/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add legacy compatibility boundary and guidance</name>
  <files>src/legacy/compatibility.ts, src/cli/handlers.ts, src/errors/cli-errors.ts</files>
  <action>Implement a dedicated compatibility module that detects legacy invocation patterns and returns concise migration guidance. Keep messages operational and deterministic, with explicit next command examples. Ensure this path is guidance-only and does not delegate back to Python.</action>
  <verify><automated>bun test test/cli/identity.test.ts</automated></verify>
  <done>Legacy invocations are recognized and produce deterministic migration guidance with non-zero exit where appropriate.</done>
</task>

<task type="auto">
  <name>Task 2: Make Bun/TS the authoritative default runtime path</name>
  <files>package.json, src/index.ts, README.md</files>
  <action>Update package scripts/bin wiring so the default `pi-tube` command path is Bun/TS without Python runtime assumptions. Remove references that imply Python is required for primary execution while preserving any needed migration notes for users still on old setups.</action>
  <verify><automated>bun run --bun bin/pi-tube.ts --version</automated></verify>
  <done>Primary command path and package metadata no longer require Python runtime for v1 execution path.</done>
</task>

<task type="auto">
  <name>Task 3: Add migration guard test for no-Python primary path</name>
  <files>test/cli/no-python-runtime.test.ts</files>
  <action>Create integration-oriented test coverage that validates baseline command execution does not invoke Python. The test should fail if runtime path regresses to shelling out to `python`, `pip`, or Typer-based entry behavior in the default path.</action>
  <verify><automated>bun test test/cli/no-python-runtime.test.ts</automated></verify>
  <done>Test reliably detects Python fallback regressions and passes on Bun-first runtime path.</done>
</task>

</tasks>

<verification>
- [ ] `bun run --bun bin/pi-tube.ts --version` executes in Bun path
- [ ] `bun test test/cli/no-python-runtime.test.ts` passes
- [ ] Compatibility guidance exists for legacy invocation patterns
</verification>

<success_criteria>

- All tasks completed
- MIGR-03 is covered with runtime path + regression test
- Legacy compatibility messaging is explicit and deterministic
- Default CLI path remains `pi-tube` on Bun/TS

</success_criteria>

<output>
After completion, create `.planning/phases/01-bun-ts-foundation/01-03-SUMMARY.md`
</output>
