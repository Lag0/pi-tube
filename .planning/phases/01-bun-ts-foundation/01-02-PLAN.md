---
phase: 01-bun-ts-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/cli/command-contract.ts
  - src/cli/build-cli.ts
  - src/cli/handlers.ts
  - src/errors/cli-errors.ts
  - test/cli/help.test.ts
  - test/cli/identity.test.ts
autonomous: true
requirements: [CLI-01, MIGR-02]
user_setup: []
must_haves:
  truths:
    - "`pi-tube --help` shows the locked section order: Usage -> Commands -> Global options -> Examples -> Notes"
    - "Top-level help includes 3-4 high-signal examples and labels future capabilities as coming soon"
    - "Baseline command surface is `pi-tube <input>` plus deterministic not-implemented guidance for deferred paths"
  artifacts:
    - "src/cli/build-cli.ts"
    - "src/cli/handlers.ts"
    - "test/cli/help.test.ts"
  key_links:
    - "Help renderer consumes section order/constants from command-contract.ts"
    - "Unimplemented command handlers map through shared CLI error types"
---

<objective>
Port base CLI routing and shared help/flags UX according to locked context decisions.

Purpose: Deliver discoverable CLI behavior now without implementing later-phase source/provider functionality.
Output: Deterministic `--help`, stable baseline command routing, and regression tests for identity/help contract.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bun-ts-foundation/01-CONTEXT.md
@.planning/phases/01-bun-ts-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement locked help information architecture</name>
  <files>src/cli/command-contract.ts, src/cli/build-cli.ts</files>
  <action>Implement top-level help output using the exact locked structure (Usage, Commands, Global options, Examples, Notes). Add 3-4 high-signal examples and explicitly label later-phase source paths as "coming soon". Keep tone concise and operational with copy-paste-friendly commands.</action>
  <verify><automated>bun run --bun bin/pi-tube.ts --help</automated></verify>
  <done>Help output includes required sections in order, example density, and coming-soon labeling.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce baseline command surface and deterministic placeholder behavior</name>
  <files>src/cli/build-cli.ts, src/cli/handlers.ts, src/errors/cli-errors.ts</files>
  <action>Route the baseline contract through `pi-tube <input>` and implement deterministic not-implemented responses for surfaced but not-yet-built paths. Ensure non-zero exits and phase-aware guidance, never silent success/no-op for placeholders.</action>
  <verify><automated>bun test test/cli/help.test.ts</automated></verify>
  <done>Unimplemented flows return explicit deterministic failures and baseline positional contract remains stable.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract tests for help and command identity</name>
  <files>test/cli/help.test.ts, test/cli/identity.test.ts</files>
  <action>Add Bun tests that snapshot/validate help section ordering and confirm exposed command identity is still `pi-tube`. Include assertions that coming-soon labeling appears for deferred source types and that placeholder paths are non-zero with actionable guidance.</action>
  <verify><automated>bun test test/cli/help.test.ts test/cli/identity.test.ts</automated></verify>
  <done>Help/identity tests pass and fail deterministically when section order or naming regresses.</done>
</task>

</tasks>

<verification>
- [ ] `bun run --bun bin/pi-tube.ts --help` shows required structure
- [ ] `bun test test/cli/help.test.ts test/cli/identity.test.ts` passes
- [ ] Placeholder command paths exit non-zero with deterministic guidance
</verification>

<success_criteria>

- All tasks completed
- CLI discoverability requirement (CLI-01) is covered by behavior + tests
- Command identity `pi-tube` remains unchanged
- Placeholder behavior is deterministic and phase-aware

</success_criteria>

<output>
After completion, create `.planning/phases/01-bun-ts-foundation/01-02-SUMMARY.md`
</output>
