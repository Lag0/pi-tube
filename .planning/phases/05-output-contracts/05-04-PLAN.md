---
phase: 05-output-contracts
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/transcription/providers/index.ts
  - src/cli/handlers.ts
  - src/cli/build-cli.ts
  - src/cli/command-contract.ts
  - README.md
  - test/cli/provider-status.test.ts
  - test/cli/help.test.ts
  - test/cli/output-cli.test.ts
autonomous: true
requirements: [CLI-03, CLI-04]
user_setup: []
must_haves:
  truths:
    - "Users and agents can query provider readiness through a dedicated deterministic status command"
    - "Help and README document agent-friendly `--json` workflow usage as active behavior"
    - "Provider status output remains deterministic in both text and JSON forms"
  artifacts:
    - "src/cli/build-cli.ts"
    - "src/cli/handlers.ts"
    - "README.md"
    - "test/cli/provider-status.test.ts"
  key_links:
    - "`src/cli/build-cli.ts` routes `provider-status` to a dedicated handler in `src/cli/handlers.ts`"
    - "`src/transcription/providers/index.ts` exports provider registration metadata consumed by provider-status output"
    - "`test/cli/provider-status.test.ts` validates deterministic readiness output for both human and agent modes"
---

<objective>
Implement provider status command and finalize agent-focused CLI documentation.

Purpose: complete CLI requirements for provider readiness introspection and documented machine-usage patterns.
Output: provider-status command, updated help/README examples, and contract tests.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-output-contracts/05-RESEARCH.md
@.planning/phases/05-output-contracts/05-03-PLAN.md
@src/cli/build-cli.ts
@src/cli/handlers.ts
@src/cli/command-contract.ts
@src/transcription/providers/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic `provider-status` command for readiness inspection</name>
  <files>src/transcription/providers/index.ts, src/cli/handlers.ts, src/cli/build-cli.ts, test/cli/provider-status.test.ts</files>
  <action>Implement `pi-tube provider-status` command that reports provider IDs, whether each provider is registered, and whether required credential env vars are present. Keep output deterministic and offline (no live network checks). Support text mode and `--json` mode so agents can consume readiness information predictably.</action>
  <verify><automated>bun test test/cli/provider-status.test.ts</automated></verify>
  <done>Provider status command returns deterministic readiness data for Deepgram and Groq with stable formatting in text and JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI help contract and README with active `--json` + agent examples</name>
  <files>src/cli/command-contract.ts, README.md, test/cli/help.test.ts</files>
  <action>Update command/help sections to show `--json` as active, add `provider-status` command examples, and document recommended agent workflows that parse deterministic JSON output. Ensure docs include concrete command snippets for baseline transcribe + provider status in JSON mode.</action>
  <verify><automated>bun test test/cli/help.test.ts</automated></verify>
  <done>Help and README clearly document active agent-focused command paths including `--json` and provider status.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration regressions for docs-backed CLI contract flows</name>
  <files>test/cli/output-cli.test.ts, test/cli/provider-status.test.ts, src/cli/build-cli.ts</files>
  <action>Add CLI integration tests that mirror documented command examples and assert deterministic machine-parseable output. Cover at least: successful transcription with `--json`, provider-status text output, and provider-status JSON output with stable keys/ordering assumptions.</action>
  <verify><automated>bun test test/cli/output-cli.test.ts test/cli/provider-status.test.ts</automated></verify>
  <done>Documented agent workflow examples are enforced by automated tests and remain stable through future changes.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/cli/provider-status.test.ts` passes
- [ ] `bun test test/cli/help.test.ts` passes
- [ ] `bun test test/cli/output-cli.test.ts test/cli/provider-status.test.ts` passes
</verification>

<success_criteria>

- All tasks completed
- CLI-04 provider status behavior is implemented and deterministic
- CLI-03 agent-focused usage is documented and test-covered

</success_criteria>

<output>
After completion, create `.planning/phases/05-output-contracts/05-04-SUMMARY.md`
</output>
