---
phase: 05-output-contracts
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/output/json.ts
  - src/output/markdown.ts
  - src/cli/handlers.ts
  - src/cli/build-cli.ts
  - src/cli/command-contract.ts
  - test/output/json-renderer.test.ts
  - test/output/output-parity.test.ts
  - test/cli/output-cli.test.ts
autonomous: true
requirements: [OUT-04, OUT-05, OUT-06]
user_setup: []
must_haves:
  truths:
    - "User can request deterministic JSON output via `--json`"
    - "JSON output always includes `schema_version` and stable field names"
    - "Markdown and JSON are equivalent views of the same canonical artifact"
  artifacts:
    - "src/output/json.ts"
    - "src/cli/build-cli.ts"
    - "test/output/json-renderer.test.ts"
    - "test/output/output-parity.test.ts"
  key_links:
    - "`src/cli/build-cli.ts` routes `--json` requests through `renderJson()` in `src/output/json.ts`"
    - "`test/output/output-parity.test.ts` compares canonical artifact equivalence across `src/output/markdown.ts` and `src/output/json.ts`"
---

<objective>
Implement deterministic JSON rendering and activate the `--json` output path.

Purpose: deliver agent-focused machine output with schema versioning while preserving one canonical data source.
Output: JSON renderer, CLI `--json` integration, and parity regressions.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-output-contracts/05-RESEARCH.md
@.planning/phases/05-output-contracts/05-01-PLAN.md
@.planning/phases/05-output-contracts/05-02-PLAN.md
@src/output/markdown.ts
@src/cli/build-cli.ts
@src/cli/handlers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic JSON renderer from canonical artifact</name>
  <files>src/output/json.ts, test/output/json-renderer.test.ts</files>
  <action>Implement `renderJson(artifact)` that serializes canonical output with deterministic property ordering, explicit `schema_version`, and no provider-native leakage. Ensure optional fields follow deterministic omission/null rules and add tests that lock field names/shape and schema version expectations.</action>
  <verify><automated>bun test test/output/json-renderer.test.ts</automated></verify>
  <done>JSON renderer emits deterministic contract output with schema version and stable field names.</done>
</task>

<task type="auto">
  <name>Task 2: Activate `--json` CLI execution path</name>
  <files>src/cli/handlers.ts, src/cli/build-cli.ts, src/cli/command-contract.ts, test/cli/output-cli.test.ts</files>
  <action>Replace Phase 4 `--json` planned-feature failure with active output mode. Keep baseline input/transcription flow unchanged, then return either Markdown or JSON from the same canonical artifact depending on flag state. Update help text and examples to mark `--json` as active and deterministic for agent workflows.</action>
  <verify><automated>bun test test/cli/output-cli.test.ts</automated></verify>
  <done>`--json` runs successfully and returns deterministic JSON contract without changing non-json behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add parity tests asserting Markdown/JSON equivalence</name>
  <files>src/output/markdown.ts, test/output/output-parity.test.ts, test/cli/output-cli.test.ts</files>
  <action>Add parity tests that build one canonical artifact and assert semantic equivalence between Markdown and JSON outputs for metadata, summary, transcript content, and segment/timestamp information. Include CLI-level tests for both output modes on the same input fixture to protect against drift.</action>
  <verify><automated>bun test test/output/output-parity.test.ts test/cli/output-cli.test.ts</automated></verify>
  <done>Parity regressions fail when Markdown and JSON diverge from the canonical artifact contract.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/output/json-renderer.test.ts` passes
- [ ] `bun test test/output/output-parity.test.ts test/cli/output-cli.test.ts` passes
- [ ] `--json` mode returns deterministic schema-versioned output
</verification>

<success_criteria>

- All tasks completed
- OUT-04 and OUT-05 are implemented in active CLI behavior
- OUT-06 parity is protected by automated tests

</success_criteria>

<output>
After completion, create `.planning/phases/05-output-contracts/05-03-SUMMARY.md`
</output>
