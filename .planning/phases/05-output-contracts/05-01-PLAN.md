---
phase: 05-output-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/output/contract.ts
  - src/output/build-artifact.ts
  - src/transcription/types.ts
  - src/transcription/providers/provider.ts
  - src/transcription/providers/deepgram.ts
  - src/transcription/providers/groq.ts
  - src/transcription/service.ts
  - test/output/output-contract.test.ts
autonomous: true
requirements: [OUT-03, OUT-05, OUT-06]
user_setup: []
must_haves:
  truths:
    - "One canonical artifact model exists as the single source of truth for all output formats"
    - "Transcript segment/timestamp data has a stable optional representation when providers expose it"
    - "Schema version is centrally defined for deterministic renderer contracts"
  artifacts:
    - "src/output/contract.ts"
    - "src/output/build-artifact.ts"
    - "src/transcription/types.ts"
    - "test/output/output-contract.test.ts"
  key_links:
    - "`src/transcription/service.ts` returns data that `buildOutputArtifact()` in `src/output/build-artifact.ts` converts into the canonical output contract"
    - "Provider adapters in `src/transcription/providers/*.ts` normalize segment/language fields consumed by `src/output/contract.ts`"
---

<objective>
Define and implement the canonical output artifact contract used by both Markdown and JSON renderers.

Purpose: create one deterministic source model before any renderer logic so parity can be enforced by design.
Output: typed output contract + artifact builder + contract tests.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-output-contracts/05-RESEARCH.md
@src/transcription/types.ts
@src/transcription/service.ts
@src/transcription/providers/provider.ts
@src/transcription/providers/deepgram.ts
@src/transcription/providers/groq.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical output schema and versioning primitives</name>
  <files>src/output/contract.ts, src/output/build-artifact.ts</files>
  <action>Create canonical output types with deterministic field names and `schema_version` constant (for example `1.0.0`). Include metadata, summary structure, transcript body, optional segments (`start_ms`, `end_ms`, `text`), source/provider/language context, and generated timestamp. Implement a builder helper that takes current transcription execution data and produces the canonical artifact shape without output-format assumptions.</action>
  <verify><automated>bun test test/output/output-contract.test.ts</automated></verify>
  <done>Canonical contract type definitions compile and artifact builder returns a stable typed object with deterministic keys and schema version.</done>
</task>

<task type="auto">
  <name>Task 2: Extend transcription normalization for optional segment/timestamp support</name>
  <files>src/transcription/types.ts, src/transcription/providers/provider.ts, src/transcription/providers/deepgram.ts, src/transcription/providers/groq.ts, src/transcription/service.ts</files>
  <action>Extend provider/transcription contracts to carry optional normalized transcript segments when available from provider payloads. Keep existing transcript text contract stable while adding optional timestamped segment array and preserving provider/language metadata behavior from Phase 4. Ensure providers without segments still return deterministic empty/undefined segment behavior.</action>
  <verify><automated>bun test test/transcription/deepgram-provider.test.ts test/transcription/groq-provider.test.ts test/transcription/transcription-service.test.ts</automated></verify>
  <done>Service-level transcription result can carry optional segment/timestamp data without breaking existing provider selection and error behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add canonical output contract regression tests</name>
  <files>test/output/output-contract.test.ts</files>
  <action>Add tests validating deterministic field naming, schema-version presence, segment normalization, and canonical artifact creation from provider/service result inputs. Cover both segment-present and segment-absent payloads to protect `OUT-03` + `OUT-06` prerequisites before renderer implementation.</action>
  <verify><automated>bun test test/output/output-contract.test.ts</automated></verify>
  <done>Contract tests fail on schema drift and prove one canonical artifact can represent all required output information.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/output/output-contract.test.ts` passes
- [ ] Provider/service contracts support optional normalized transcript segments
- [ ] Canonical artifact schema includes deterministic `schema_version`
</verification>

<success_criteria>

- All tasks completed
- Canonical output contract and builder are ready for renderer implementation
- Output data model is deterministic and parity-friendly across future formats

</success_criteria>

<output>
After completion, create `.planning/phases/05-output-contracts/05-01-SUMMARY.md`
</output>
