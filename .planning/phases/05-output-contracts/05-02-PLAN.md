---
phase: 05-output-contracts
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/output/markdown.ts
  - src/output/build-artifact.ts
  - src/cli/handlers.ts
  - src/cli/build-cli.ts
  - src/cli/command-contract.ts
  - test/output/markdown-renderer.test.ts
  - test/cli/output-cli.test.ts
autonomous: true
requirements: [OUT-01, OUT-02, OUT-03, OUT-06]
user_setup: []
must_haves:
  truths:
    - "Default successful CLI output is deterministic Markdown with YAML frontmatter"
    - "Summary format is fixed to one paragraph plus exactly five bullet points"
    - "Transcript section includes timestamp lines when segment data exists"
  artifacts:
    - "src/output/markdown.ts"
    - "src/cli/handlers.ts"
    - "test/output/markdown-renderer.test.ts"
    - "test/cli/output-cli.test.ts"
  key_links:
    - "`src/cli/handlers.ts` builds the canonical artifact then renders it through `renderMarkdown()` in `src/output/markdown.ts`"
    - "`test/cli/output-cli.test.ts` verifies CLI default output shape produced by `src/output/markdown.ts`"
---

<objective>
Implement deterministic Markdown rendering and wire it as the default successful output mode.

Purpose: satisfy frontmatter + summary + transcript structure requirements while preserving baseline CLI command contract.
Output: Markdown renderer, CLI integration, and regression tests.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-output-contracts/05-RESEARCH.md
@.planning/phases/05-output-contracts/05-01-PLAN.md
@src/cli/handlers.ts
@src/cli/build-cli.ts
@src/cli/command-contract.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic Markdown renderer with YAML frontmatter and fixed summary block</name>
  <files>src/output/markdown.ts, src/output/build-artifact.ts</files>
  <action>Implement `renderMarkdown(artifact)` that emits deterministic YAML frontmatter fields, then summary block (exactly one paragraph with 2-4 sentences plus exactly 5 bullet points), then transcript sections. Normalize spacing/newlines so output is stable across repeated executions with the same artifact input.</action>
  <verify><automated>bun test test/output/markdown-renderer.test.ts</automated></verify>
  <done>Markdown renderer outputs contract-compliant frontmatter and fixed summary structure from canonical artifact input.</done>
</task>

<task type="auto">
  <name>Task 2: Render transcript section with timestamp-aware formatting</name>
  <files>src/output/markdown.ts, test/output/markdown-renderer.test.ts</files>
  <action>Add transcript rendering rules: if canonical segments exist, render each line with deterministic timestamp labels and text ordering; otherwise render plain transcript fallback section. Keep one stable heading structure regardless of provider. Add unit tests for both segmented and non-segmented transcript cases.</action>
  <verify><automated>bun test test/output/markdown-renderer.test.ts</automated></verify>
  <done>Markdown transcript section deterministically includes timestamped entries when segment data is available and stable fallback when not.</done>
</task>

<task type="auto">
  <name>Task 3: Wire default CLI success output to Markdown contract path</name>
  <files>src/cli/handlers.ts, src/cli/build-cli.ts, src/cli/command-contract.ts, test/cli/output-cli.test.ts</files>
  <action>Update baseline success path to build canonical artifact and print Markdown output by default. Preserve existing non-zero error behavior and command parsing. Update help/contract phrasing to reflect active Markdown output mode in Phase 5. Add CLI integration tests asserting frontmatter presence and fixed summary/transcript sections.</action>
  <verify><automated>bun test test/cli/output-cli.test.ts</automated></verify>
  <done>Default CLI successful runs output deterministic Markdown contract with no regression to command/error behavior.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/output/markdown-renderer.test.ts` passes
- [ ] `bun test test/cli/output-cli.test.ts` passes
- [ ] Default CLI output includes YAML frontmatter and fixed summary format
</verification>

<success_criteria>

- All tasks completed
- OUT-01, OUT-02, and OUT-03 are implemented for default Markdown mode
- Markdown output is deterministic and built from canonical artifact model

</success_criteria>

<output>
After completion, create `.planning/phases/05-output-contracts/05-02-SUMMARY.md`
</output>
