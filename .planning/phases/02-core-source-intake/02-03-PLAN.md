---
phase: 02-core-source-intake
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/intake/adapters/direct-url.ts
  - src/intake/adapters/local-file.ts
  - src/intake/policy.ts
  - test/intake/direct-url-adapter.test.ts
  - test/intake/local-file-adapter.test.ts
autonomous: true
requirements: [SRC-04, SRC-05, SRC-06]
user_setup: []
must_haves:
  truths:
    - "Direct downloadable media URLs are accepted and normalized deterministically"
    - "Generic non-direct URLs are rejected early with `UNSUPPORTED_URL_NOT_DIRECT_MEDIA`"
    - "Local file paths are validated, normalized to absolute paths, and constrained to supported media extensions"
  artifacts:
    - "src/intake/adapters/direct-url.ts"
    - "src/intake/adapters/local-file.ts"
    - "test/intake/direct-url-adapter.test.ts"
    - "test/intake/local-file-adapter.test.ts"
  key_links:
    - "`src/intake/policy.ts` extension allowlists are consumed by both direct-url and local-file adapters"
    - "Adapter-level validation failures map through the stable error-code set defined in Plan 01"
---

<objective>
Implement direct-media URL and local-file intake paths.

Purpose: Satisfy SRC-04/SRC-05/SRC-06 behavior using strict deterministic policy checks.
Output: Non-YouTube core intake adapters with full validation and regression tests.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-source-intake/02-RESEARCH.md
@.planning/phases/02-core-source-intake/02-01-PLAN.md
@src/intake/types.ts
@src/intake/policy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement direct-media URL adapter with strict policy gate</name>
  <files>src/intake/adapters/direct-url.ts, src/intake/policy.ts</files>
  <action>Implement adapter logic that accepts only URLs matching the supported direct media extension policy and rejects all other HTTP(S) URLs with `UNSUPPORTED_URL_NOT_DIRECT_MEDIA`. Keep behavior deterministic and avoid network-dependent validation in this phase.</action>
  <verify><automated>bun test test/intake/direct-url-adapter.test.ts</automated></verify>
  <done>Direct URL adapter accepts supported media URLs and deterministically rejects unsupported URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement local-file adapter with normalization and format checks</name>
  <files>src/intake/adapters/local-file.ts, src/intake/policy.ts</files>
  <action>Implement local file intake that resolves absolute paths, validates file existence, and enforces supported audio/video extensions used by project requirements. Return normalized source contract data for downstream phases.</action>
  <verify><automated>bun test test/intake/local-file-adapter.test.ts</automated></verify>
  <done>Local file adapter returns normalized source metadata for valid files and deterministic failures for missing/unsupported files.</done>
</task>

<task type="auto">
  <name>Task 3: Add direct/local adapter tests for policy and normalization</name>
  <files>test/intake/direct-url-adapter.test.ts, test/intake/local-file-adapter.test.ts</files>
  <action>Create focused bun tests for positive and negative cases across direct URL and local file inputs, asserting stable error codes and normalized contract fields.</action>
  <verify><automated>bun test test/intake/direct-url-adapter.test.ts test/intake/local-file-adapter.test.ts</automated></verify>
  <done>Adapter test suites pass and enforce SRC-04/SRC-05/SRC-06 constraints.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/intake/direct-url-adapter.test.ts test/intake/local-file-adapter.test.ts` passes
- [ ] Unsupported non-direct URLs fail with `UNSUPPORTED_URL_NOT_DIRECT_MEDIA`
- [ ] Local file intake normalizes to absolute path and validates format support
</verification>

<success_criteria>

- All tasks completed
- Direct URL and local file intake paths are implemented and deterministic
- SRC-04, SRC-05, and SRC-06 are covered by automated tests

</success_criteria>

<output>
After completion, create `.planning/phases/02-core-source-intake/02-03-SUMMARY.md`
</output>
