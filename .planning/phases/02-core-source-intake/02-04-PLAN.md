---
phase: 02-core-source-intake
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/cli/handlers.ts
  - src/cli/build-cli.ts
  - src/intake/resolver.ts
  - src/errors/cli-errors.ts
  - test/intake/intake-matrix.test.ts
  - test/cli/intake-cli.test.ts
autonomous: true
requirements: [SRC-01, SRC-04, SRC-05, SRC-06]
user_setup: []
must_haves:
  truths:
    - "`pi-tube <input>` executes real Phase 2 intake logic for YouTube, direct media URLs, and local files"
    - "Unsupported non-direct URLs fail at CLI boundary with `UNSUPPORTED_URL_NOT_DIRECT_MEDIA`"
    - "Core source matrix behavior is covered by integration tests"
  artifacts:
    - "src/cli/handlers.ts"
    - "src/intake/resolver.ts"
    - "test/intake/intake-matrix.test.ts"
    - "test/cli/intake-cli.test.ts"
  key_links:
    - "`src/cli/handlers.ts` baseline input path calls `resolveSource()` and maps deterministic intake results/errors"
    - "Integration tests exercise CLI -> resolver -> adapter flow for each Phase 2 source class"
---

<objective>
Wire source intake into CLI execution and finalize regression matrix coverage.

Purpose: Complete Phase 2 by making the baseline input command execute deterministic intake logic and proving requirement coverage through integration tests.
Output: CLI-integrated intake pipeline and source-matrix regression tests.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-source-intake/02-RESEARCH.md
@.planning/phases/02-core-source-intake/02-02-PLAN.md
@.planning/phases/02-core-source-intake/02-03-PLAN.md
@src/cli/build-cli.ts
@src/cli/handlers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate resolver into baseline CLI input handler</name>
  <files>src/cli/handlers.ts, src/intake/resolver.ts, src/errors/cli-errors.ts</files>
  <action>Replace the current Phase 1 placeholder-only baseline execution path with Phase 2 intake flow. Baseline handler should call `resolveSource(input)`, preserve deterministic error mapping, and provide concise operational output for successful intake classification while keeping provider execution deferred to later phases.</action>
  <verify><automated>bun test test/cli/intake-cli.test.ts</automated></verify>
  <done>Baseline CLI path uses real intake resolution and deterministic errors instead of unconditional Phase 1 placeholder failure.</done>
</task>

<task type="auto">
  <name>Task 2: Add source matrix integration tests</name>
  <files>test/intake/intake-matrix.test.ts, test/cli/intake-cli.test.ts</files>
  <action>Create integration coverage for the full matrix: valid YouTube URL, valid direct media URL, unsupported non-direct URL (`UNSUPPORTED_URL_NOT_DIRECT_MEDIA`), and valid local file path. Assert exit codes, key output markers, and machine-readable error codes.</action>
  <verify><automated>bun test test/intake/intake-matrix.test.ts test/cli/intake-cli.test.ts</automated></verify>
  <done>Matrix tests pass and fail deterministically on source-intake regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize CLI contract text for Phase 2 behavior</name>
  <files>src/cli/build-cli.ts, src/cli/handlers.ts</files>
  <action>Update CLI help/notes and baseline guidance text to reflect Phase 2 intake support while preserving accurate phase labels for still-deferred behavior (Instagram/public policy and provider execution). Ensure contract wording remains concise and operational.</action>
  <verify><automated>bun test test/cli/help.test.ts test/cli/intake-cli.test.ts</automated></verify>
  <done>Help and baseline behavior messaging align with implemented Phase 2 scope and tests remain green.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/intake/intake-matrix.test.ts test/cli/intake-cli.test.ts` passes
- [ ] Baseline CLI input path resolves supported source classes deterministically
- [ ] Unsupported non-direct URLs fail with `UNSUPPORTED_URL_NOT_DIRECT_MEDIA`
</verification>

<success_criteria>

- All tasks completed
- Phase 2 core source intake matrix is executable via `pi-tube <input>`
- All Phase 2 requirements are covered by integration-oriented tests

</success_criteria>

<output>
After completion, create `.planning/phases/02-core-source-intake/02-04-SUMMARY.md`
</output>
