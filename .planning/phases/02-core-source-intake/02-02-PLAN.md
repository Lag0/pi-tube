---
phase: 02-core-source-intake
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/intake/adapters/youtube.ts
  - src/intake/tools/yt-dlp.ts
  - test/intake/youtube-adapter.test.ts
autonomous: true
requirements: [SRC-01]
user_setup: []
must_haves:
  truths:
    - "A YouTube URL resolves through a deterministic adapter boundary suitable for downstream transcription"
    - "YouTube extraction failures map to stable machine-readable CLI errors"
    - "Missing `yt-dlp` tooling produces explicit remediation guidance"
  artifacts:
    - "src/intake/adapters/youtube.ts"
    - "src/intake/tools/yt-dlp.ts"
    - "test/intake/youtube-adapter.test.ts"
  key_links:
    - "`src/intake/adapters/youtube.ts` delegates external extraction to `src/intake/tools/yt-dlp.ts`"
    - "Adapter failure modes map to stable error codes defined by the shared intake contract from Plan 01"
---

<objective>
Implement the YouTube intake adapter and extraction boundary.

Purpose: Satisfy SRC-01 intake behavior with deterministic handling of success and known failure classes.
Output: YouTube adapter integrated behind the shared resolver contract with automated adapter tests.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-source-intake/02-RESEARCH.md
@.planning/phases/02-core-source-intake/02-01-PLAN.md
@src/intake/types.ts
@src/intake/resolver.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build yt-dlp subprocess boundary with deterministic error mapping</name>
  <files>src/intake/tools/yt-dlp.ts</files>
  <action>Implement a focused yt-dlp execution module using `Bun.spawn` for metadata/media resolution. Map failure classes (missing binary, non-zero exit, malformed output) to the stable error codes defined in Plan 01, with concise remediation guidance.</action>
  <verify><automated>bun test test/intake/youtube-adapter.test.ts</automated></verify>
  <done>yt-dlp boundary returns typed success data and deterministic typed failures.</done>
</task>

<task type="auto">
  <name>Task 2: Implement YouTube adapter over the shared intake contract</name>
  <files>src/intake/adapters/youtube.ts</files>
  <action>Replace the Plan 01 stub with a real adapter implementation that validates YouTube URL shape, invokes yt-dlp boundary logic, and returns the normalized `ResolvedSource` structure expected by the resolver and downstream phases.</action>
  <verify><automated>bun test test/intake/youtube-adapter.test.ts</automated></verify>
  <done>YouTube adapter is non-stub, contract-compliant, and deterministic under success/failure paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add YouTube adapter test coverage</name>
  <files>test/intake/youtube-adapter.test.ts</files>
  <action>Add adapter tests that mock yt-dlp boundary outcomes for: valid URL success, missing binary failure, and extraction failure. Assert returned contract fields and error codes rather than brittle message text.</action>
  <verify><automated>bun test test/intake/youtube-adapter.test.ts</automated></verify>
  <done>Adapter test suite passes and protects deterministic YouTube intake behavior.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/intake/youtube-adapter.test.ts` passes
- [ ] YouTube URLs resolve through adapter boundary and return normalized source contract
- [ ] Missing/failed yt-dlp scenarios emit stable machine-readable error codes
</verification>

<success_criteria>

- All tasks completed
- SRC-01 intake path is implemented behind stable adapter interfaces
- YouTube adapter behavior is regression-tested

</success_criteria>

<output>
After completion, create `.planning/phases/02-core-source-intake/02-02-SUMMARY.md`
</output>
