---
phase: 02-core-source-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intake/types.ts
  - src/intake/policy.ts
  - src/intake/resolver.ts
  - src/intake/adapters/youtube.ts
  - src/intake/adapters/direct-url.ts
  - src/intake/adapters/local-file.ts
  - src/errors/cli-errors.ts
  - test/intake/source-resolver.test.ts
autonomous: true
requirements: [SRC-01, SRC-04, SRC-05, SRC-06]
user_setup: []
must_haves:
  truths:
    - "A single positional input is deterministically classified into YouTube, direct media URL, local file, or unsupported URL"
    - "Non-direct generic URLs fail early with `UNSUPPORTED_URL_NOT_DIRECT_MEDIA`"
    - "Phase 2 intake adapters share one normalized source contract for downstream phases"
  artifacts:
    - "src/intake/types.ts"
    - "src/intake/policy.ts"
    - "src/intake/resolver.ts"
    - "test/intake/source-resolver.test.ts"
  key_links:
    - "`src/cli/handlers.ts` intake callsite will consume `resolveSource()` from `src/intake/resolver.ts`"
    - "`src/intake/resolver.ts` routes classified inputs to adapter modules under `src/intake/adapters/*`"
---

<objective>
Create the intake contract and resolver foundation for Phase 2.

Purpose: Establish deterministic source classification and policy gates before source-specific adapter implementation.
Output: Typed intake domain model, resolver dispatch, deterministic unsupported URL failure path, and base resolver tests.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-source-intake/02-RESEARCH.md
@src/cli/handlers.ts
@src/errors/cli-errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define intake contracts and deterministic policy helpers</name>
  <files>src/intake/types.ts, src/intake/policy.ts, src/errors/cli-errors.ts</files>
  <action>Create normalized source types and classification helpers for `youtube`, `direct_url`, and `local_file` with a dedicated deterministic policy error for unsupported non-direct URLs (`UNSUPPORTED_URL_NOT_DIRECT_MEDIA`). Keep URL parsing based on `new URL()` and extension allowlists, not broad regex-only heuristics.</action>
  <verify><automated>bun test test/intake/source-resolver.test.ts</automated></verify>
  <done>Type contracts and policy helpers exist with stable error-code behavior for unsupported URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement resolver dispatch boundary with adapter stubs</name>
  <files>src/intake/resolver.ts, src/intake/adapters/youtube.ts, src/intake/adapters/direct-url.ts, src/intake/adapters/local-file.ts</files>
  <action>Implement `resolveSource(input)` that classifies input and dispatches to adapter boundaries. Create deterministic placeholder adapter implementations that satisfy the shared contract so Plans 02 and 03 can evolve adapters independently without changing resolver logic.</action>
  <verify><automated>bun test test/intake/source-resolver.test.ts</automated></verify>
  <done>Resolver dispatch works end-to-end with typed adapter boundaries and deterministic unsupported URL failures.</done>
</task>

<task type="auto">
  <name>Task 3: Add resolver-focused Wave 0 tests</name>
  <files>test/intake/source-resolver.test.ts</files>
  <action>Add bun tests for classification and policy outcomes: YouTube URL classification, direct URL classification by extension, unsupported URL rejection with `UNSUPPORTED_URL_NOT_DIRECT_MEDIA`, and local-file fallback. Keep tests deterministic and fast.</action>
  <verify><automated>bun test test/intake/source-resolver.test.ts</automated></verify>
  <done>Resolver test suite passes and catches unsupported URL policy regressions.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/intake/source-resolver.test.ts` passes
- [ ] `UNSUPPORTED_URL_NOT_DIRECT_MEDIA` is emitted for non-direct HTTP(S) URLs
- [ ] `resolveSource(input)` boundary exists and returns normalized source contracts
</verification>

<success_criteria>

- All tasks completed
- Intake contracts and resolver are established as stable interfaces
- Unsupported URL policy is deterministic and test-covered

</success_criteria>

<output>
After completion, create `.planning/phases/02-core-source-intake/02-01-SUMMARY.md`
</output>
