---
phase: 04-transcription-providers
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - src/cli/build-cli.ts
  - src/cli/handlers.ts
  - src/cli/command-contract.ts
  - src/transcription/service.ts
  - src/errors/cli-errors.ts
  - test/transcription/transcription-service.test.ts
  - test/cli/transcription-cli.test.ts
  - README.md
autonomous: true
requirements: [TRNS-01, TRNS-02, TRNS-03, TRNS-04]
user_setup: []
must_haves:
  truths:
    - "Provider switching between Deepgram and Groq does not change the CLI command contract or deterministic output markers"
    - "Language preference and provider/language metadata are surfaced through one canonical output contract"
    - "Provider-layer failures are exposed as stable public error codes with deterministic non-zero exits"
  artifacts:
    - "src/transcription/service.ts"
    - "src/cli/handlers.ts"
    - "test/transcription/transcription-service.test.ts"
    - "test/cli/transcription-cli.test.ts"
  key_links:
    - "`src/cli/build-cli.ts` parses provider/language options and passes them to `handleBaselineInput()` in `src/cli/handlers.ts`"
    - "`src/cli/handlers.ts` uses `src/transcription/service.ts` to execute selected provider and render provider-agnostic output"
    - "`test/cli/transcription-cli.test.ts` asserts stable output markers and error-code behavior across provider switching"
---

<objective>
Integrate provider execution into the baseline CLI flow and finalize phase-level regression coverage.

Purpose: deliver end-to-end provider selection, language metadata propagation, and stable public error mapping without contract churn.
Output: CLI/provider integration path plus comprehensive tests for TRNS-01..TRNS-04.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-transcription-providers/04-RESEARCH.md
@.planning/phases/04-transcription-providers/04-01-PLAN.md
@.planning/phases/04-transcription-providers/04-02-PLAN.md
@.planning/phases/04-transcription-providers/04-03-PLAN.md
@src/cli/build-cli.ts
@src/cli/handlers.ts
@src/cli/command-contract.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire baseline CLI flow to execute transcription with selectable provider</name>
  <files>src/cli/build-cli.ts, src/cli/handlers.ts, src/transcription/service.ts, src/cli/command-contract.ts</files>
  <action>Add provider/language option parsing while preserving `pi-tube <input>` canonical flow. Execute source intake then provider transcription through the service layer. Keep deterministic output markers and ensure provider switching does not alter the output shape; only metadata values should vary. Update help contract text to reflect active provider execution behavior.</action>
  <verify><automated>bun test test/cli/transcription-cli.test.ts</automated></verify>
  <done>CLI can select Deepgram or Groq in one baseline flow and returns stable deterministic output format across providers.</done>
</task>

<task type="auto">
  <name>Task 2: Add service integration tests for provider switching, language metadata, and fallback precedence</name>
  <files>test/transcription/transcription-service.test.ts, src/transcription/service.ts</files>
  <action>Add integration-level service tests covering: provider selection via CLI option and env fallback, language preference propagation, language metadata normalization, and identical canonical output shape across Deepgram/Groq adapters. Use deterministic adapter stubs where full network mocks are unnecessary.</action>
  <verify><automated>bun test test/transcription/transcription-service.test.ts</automated></verify>
  <done>Service-level provider switching and language semantics are deterministic and regression-protected.</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI failure mapping regressions and contract docs for provider errors</name>
  <files>test/cli/transcription-cli.test.ts, src/errors/cli-errors.ts, README.md</files>
  <action>Add CLI integration tests for representative provider failures (auth, rate limit, generic provider failure) asserting stable error codes and non-zero exits. Update README contract notes/examples to describe provider selection and stable failure semantics implemented in Phase 4.</action>
  <verify><automated>bun test test/transcription/transcription-service.test.ts test/cli/transcription-cli.test.ts</automated></verify>
  <done>TRNS-04 behavior is enforced by automated tests and user-facing documentation reflects the active provider contract.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/transcription/transcription-service.test.ts test/cli/transcription-cli.test.ts` passes
- [ ] Provider switching preserves command input/output contract shape
- [ ] Provider-layer failures map to deterministic public codes and non-zero exits
</verification>

<success_criteria>

- All tasks completed
- TRNS-01..TRNS-04 are covered by integrated service + CLI behavior
- Phase 4 can execute without requiring Phase 5 output-rendering contracts

</success_criteria>

<output>
After completion, create `.planning/phases/04-transcription-providers/04-04-SUMMARY.md`
</output>

