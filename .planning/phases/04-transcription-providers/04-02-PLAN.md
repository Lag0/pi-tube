---
phase: 04-transcription-providers
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/transcription/providers/deepgram.ts
  - src/transcription/providers/index.ts
  - src/errors/cli-errors.ts
  - src/transcription/service.ts
  - test/transcription/deepgram-provider.test.ts
autonomous: true
requirements: [TRNS-01, TRNS-03, TRNS-04]
user_setup: []
must_haves:
  truths:
    - "User can select Deepgram and receive a canonical transcription result"
    - "Language preference is passed to Deepgram requests and returned language metadata is normalized"
    - "Deepgram failures map to stable public error classes instead of raw provider messages"
  artifacts:
    - "src/transcription/providers/deepgram.ts"
    - "src/errors/cli-errors.ts"
    - "test/transcription/deepgram-provider.test.ts"
  key_links:
    - "`src/transcription/providers/index.ts` registers the Deepgram adapter for service-level provider selection"
    - "`src/transcription/providers/deepgram.ts` maps provider failure statuses into stable constructors in `src/errors/cli-errors.ts`"
---

<objective>
Implement Deepgram provider adapter with canonical options and metadata mapping.

Purpose: deliver TRNS-01 while preserving the provider-agnostic contract from plan 01.
Output: Deepgram adapter, deterministic error mapping, and unit coverage for request/response normalization.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-transcription-providers/04-RESEARCH.md
@.planning/phases/04-transcription-providers/04-01-PLAN.md
@src/errors/cli-errors.ts
@src/transcription/types.ts
@src/transcription/service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Deepgram adapter implementing canonical provider interface</name>
  <files>src/transcription/providers/deepgram.ts, src/transcription/providers/index.ts</files>
  <action>Implement Deepgram adapter with Bun `fetch`/`FormData` boundary (or equivalent injected HTTP client) that accepts canonical request fields and maps successful responses to canonical transcript text + language metadata fields. Ensure request payload carries optional language preference when provided and keeps auto-detection behavior when absent.</action>
  <verify><automated>bun test test/transcription/deepgram-provider.test.ts</automated></verify>
  <done>Deepgram adapter produces canonical transcription results and is registered for service-level selection.</done>
</task>

<task type="auto">
  <name>Task 2: Map Deepgram-specific failures to stable public error classes</name>
  <files>src/errors/cli-errors.ts, src/transcription/providers/deepgram.ts, src/transcription/service.ts</files>
  <action>Add deterministic error constructors for provider auth, rate-limit, unavailable, and generic transcribe-failure classes as needed by the service contract. Map Deepgram HTTP/status failure modes to these constructors and preserve provider context for troubleshooting without leaking provider-native message contracts.</action>
  <verify><automated>bun test test/transcription/deepgram-provider.test.ts</automated></verify>
  <done>Deepgram failures are surfaced through stable public codes and deterministic guidance text.</done>
</task>

<task type="auto">
  <name>Task 3: Add Deepgram adapter regression tests for language and error mapping</name>
  <files>test/transcription/deepgram-provider.test.ts</files>
  <action>Add tests for successful transcription normalization, explicit language preference pass-through, detected language metadata mapping, and representative failure mapping to stable codes. Use injected fetch stubs to avoid network dependence.</action>
  <verify><automated>bun test test/transcription/deepgram-provider.test.ts</automated></verify>
  <done>Deepgram adapter behavior is deterministic and protected by fast unit tests.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/transcription/deepgram-provider.test.ts` passes
- [ ] Deepgram selection path returns canonical result contract
- [ ] Deepgram failure modes map to stable public error codes
</verification>

<success_criteria>

- All tasks completed
- TRNS-01 implemented via Deepgram adapter behind canonical provider contract
- Language preference and metadata behavior for Deepgram is test-covered and deterministic

</success_criteria>

<output>
After completion, create `.planning/phases/04-transcription-providers/04-02-SUMMARY.md`
</output>

