---
phase: 04-transcription-providers
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/transcription/providers/groq.ts
  - src/transcription/providers/index.ts
  - src/errors/cli-errors.ts
  - src/transcription/service.ts
  - test/transcription/groq-provider.test.ts
autonomous: true
requirements: [TRNS-02, TRNS-03, TRNS-04]
user_setup: []
must_haves:
  truths:
    - "User can select Groq and receive a canonical transcription result"
    - "Language preference is forwarded to Groq and returned language metadata is normalized"
    - "Groq failures map to stable public error classes instead of provider-native message strings"
  artifacts:
    - "src/transcription/providers/groq.ts"
    - "src/errors/cli-errors.ts"
    - "test/transcription/groq-provider.test.ts"
  key_links:
    - "`src/transcription/providers/index.ts` registers the Groq adapter for service-level provider selection"
    - "`src/transcription/providers/groq.ts` maps provider failures through constructors in `src/errors/cli-errors.ts`"
---

<objective>
Implement Groq provider adapter with canonical options and metadata mapping.

Purpose: deliver TRNS-02 while preserving the provider-agnostic contract and error taxonomy.
Output: Groq adapter, deterministic provider failure mapping, and unit coverage for language + metadata behavior.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-transcription-providers/04-RESEARCH.md
@.planning/phases/04-transcription-providers/04-01-PLAN.md
@src/errors/cli-errors.ts
@src/transcription/types.ts
@src/transcription/service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Groq adapter implementing canonical provider interface</name>
  <files>src/transcription/providers/groq.ts, src/transcription/providers/index.ts</files>
  <action>Implement Groq adapter with Bun `fetch`/`FormData` boundary (or equivalent injected HTTP client) that maps provider responses to canonical transcript text + language metadata fields. Ensure optional language preference is passed when provided and fallback behavior remains deterministic when omitted.</action>
  <verify><automated>bun test test/transcription/groq-provider.test.ts</automated></verify>
  <done>Groq adapter returns canonical transcription results and is registered for provider selection.</done>
</task>

<task type="auto">
  <name>Task 2: Map Groq-specific failures to stable public error classes</name>
  <files>src/errors/cli-errors.ts, src/transcription/providers/groq.ts, src/transcription/service.ts</files>
  <action>Map Groq failure classes (auth, rate limit, provider unavailable, malformed response, generic transcribe failure) into stable public `CliError` constructors shared by the provider service. Keep provider detail only as optional context in message/guidance, not as brittle parsing contract.</action>
  <verify><automated>bun test test/transcription/groq-provider.test.ts</automated></verify>
  <done>Groq failure behavior is deterministic and aligned with shared public error taxonomy.</done>
</task>

<task type="auto">
  <name>Task 3: Add Groq adapter regression tests for language and failure mapping</name>
  <files>test/transcription/groq-provider.test.ts</files>
  <action>Add tests for successful canonical result mapping, language preference pass-through, language metadata extraction, and deterministic error-code mapping for representative failure cases. Use injected fetch stubs for deterministic test speed.</action>
  <verify><automated>bun test test/transcription/groq-provider.test.ts</automated></verify>
  <done>Groq adapter contract is test-protected and stable across provider response variance.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/transcription/groq-provider.test.ts` passes
- [ ] Groq selection path returns canonical result contract
- [ ] Groq failure modes map to stable public error codes
</verification>

<success_criteria>

- All tasks completed
- TRNS-02 implemented via Groq adapter behind canonical provider contract
- Language preference and metadata behavior for Groq is test-covered and deterministic

</success_criteria>

<output>
After completion, create `.planning/phases/04-transcription-providers/04-03-SUMMARY.md`
</output>

