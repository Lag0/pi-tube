---
phase: 04-transcription-providers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/transcription/types.ts
  - src/transcription/providers/provider.ts
  - src/transcription/providers/index.ts
  - src/transcription/service.ts
  - src/cli/handlers.ts
  - test/transcription/provider-contract.test.ts
autonomous: true
requirements: [TRNS-03, TRNS-04]
user_setup: []
must_haves:
  truths:
    - "Transcription execution is defined by one provider-agnostic contract regardless of provider choice"
    - "Language preference and language metadata fields are canonicalized in one response shape"
    - "Provider-specific failures can be mapped through stable public error constructors"
  artifacts:
    - "src/transcription/types.ts"
    - "src/transcription/providers/provider.ts"
    - "src/transcription/service.ts"
    - "test/transcription/provider-contract.test.ts"
  key_links:
    - "`src/cli/handlers.ts` delegates transcription execution to `transcribeFromResolvedSource()` in `src/transcription/service.ts`"
    - "`src/transcription/service.ts` consumes providers through the interface exported by `src/transcription/providers/provider.ts`"
---

<objective>
Define the canonical transcription provider contract and service boundary.

Purpose: establish provider-agnostic request/result types and service wiring before provider-specific adapters are implemented.
Output: executable contract layer used by Deepgram and Groq plans without changing CLI input/output structure.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-transcription-providers/04-RESEARCH.md
@src/intake/types.ts
@src/cli/handlers.ts
@src/errors/cli-errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical transcription request/result contracts and provider interface</name>
  <files>src/transcription/types.ts, src/transcription/providers/provider.ts, src/transcription/providers/index.ts</files>
  <action>Create typed provider-agnostic contracts (`TranscriptionProviderId`, `TranscriptionRequest`, `TranscriptionResult`, and metadata fields including requested/detected language). Define a provider interface with one transcribe method and explicit error boundary expectations. Export a registry surface that later plans can populate with Deepgram and Groq adapters.</action>
  <verify><automated>bun test test/transcription/provider-contract.test.ts</automated></verify>
  <done>Contract types and provider interface compile and represent language + metadata needs for both providers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement transcription service skeleton with provider selection and stable result envelope</name>
  <files>src/transcription/service.ts, src/cli/handlers.ts</files>
  <action>Create `transcribeFromResolvedSource()` service that accepts resolved intake output, provider selection input, and optional language preference. Enforce deterministic provider selection precedence (`--provider` input first, then env fallback, then default) and return canonical result envelope without provider-specific shape leakage. Keep CLI baseline input path unchanged.</action>
  <verify><automated>bun test test/transcription/provider-contract.test.ts</automated></verify>
  <done>Service layer is callable from CLI handlers and provides a stable provider-agnostic contract surface for future adapters.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract regression tests for provider-agnostic shape and selection precedence</name>
  <files>test/transcription/provider-contract.test.ts</files>
  <action>Add tests that validate canonical result shape, deterministic provider selection precedence, and language metadata field presence/absence behavior. Use stubs for provider implementations to keep tests fast and isolated from network behavior.</action>
  <verify><automated>bun test test/transcription/provider-contract.test.ts</automated></verify>
  <done>Contract tests prevent regression in provider-agnostic response shape and selection behavior before adapter implementation.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/transcription/provider-contract.test.ts` passes
- [ ] Service-level provider selection is deterministic and documented in code
- [ ] Canonical transcription result fields cover requested/detected language semantics
</verification>

<success_criteria>

- All tasks completed
- Provider abstraction and canonical response contract are in place for both provider implementations
- CLI wiring stays on `pi-tube <input>` baseline contract while enabling provider execution

</success_criteria>

<output>
After completion, create `.planning/phases/04-transcription-providers/04-01-SUMMARY.md`
</output>

