---
phase: 06-reliability-release-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/types.ts
  - src/config/store.ts
  - src/transcription/service.ts
  - src/cli/build-cli.ts
  - src/cli/handlers.ts
  - src/cli/command-contract.ts
  - README.md
  - test/cli/config-cli.test.ts
autonomous: true
requirements: [CLI-02]
user_setup: []
must_haves:
  truths:
    - "User can configure provider defaults through a deterministic CLI command flow"
    - "Provider/language resolution precedence is explicit and testable across CLI flags, config, and env"
    - "Config command behavior is documented in help and README contract examples"
  artifacts:
    - "src/config/store.ts"
    - "src/cli/build-cli.ts"
    - "test/cli/config-cli.test.ts"
  key_links:
    - "`src/cli/build-cli.ts` routes `config` subcommands to config store operations in `src/config/store.ts`"
    - "`src/transcription/service.ts` consumes resolved defaults from config/env precedence chain"
---

<objective>
Implement provider configuration UX and deterministic precedence rules.

Purpose: satisfy CLI-02 by adding a reliable config command path instead of env-only setup.
Output: config storage/resolution module + CLI command wiring + regression tests/docs.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reliability-release-gates/06-RESEARCH.md
@.planning/phases/06-reliability-release-gates/06-VALIDATION.md
@src/cli/build-cli.ts
@src/cli/handlers.ts
@src/transcription/service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config persistence and typed config contract</name>
  <files>src/config/types.ts, src/config/store.ts</files>
  <action>Create a typed config model supporting provider defaults and credentials, with deterministic read/write behavior and non-destructive updates. Include explicit path resolution suitable for local development and test isolation.</action>
  <verify><automated>bun test test/cli/config-cli.test.ts</automated></verify>
  <done>Config store supports deterministic set/get/list operations for provider-related keys with stable serialization behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement `pi-tube config` command flow and precedence integration</name>
  <files>src/cli/build-cli.ts, src/cli/handlers.ts, src/transcription/service.ts, test/cli/config-cli.test.ts</files>
  <action>Add a `config` command surface for setting/getting provider defaults and credential references. Integrate precedence rules in runtime selection flow (CLI flag > config > env default where applicable). Ensure deterministic output in text and `--json` mode for config commands.</action>
  <verify><automated>bun test test/cli/config-cli.test.ts test/transcription/transcription-service.test.ts</automated></verify>
  <done>Users can set deterministic defaults through CLI config and runtime honors explicit precedence without breaking existing transcription behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Update help/README with config workflow examples</name>
  <files>src/cli/command-contract.ts, README.md, test/cli/help.test.ts, test/cli/install-flow.test.ts</files>
  <action>Document config command usage for provider defaults/credentials and show concrete setup flows for agent/human usage. Align help and README examples with implemented config command behavior.</action>
  <verify><automated>bun test test/cli/help.test.ts test/cli/install-flow.test.ts</automated></verify>
  <done>Config command is discoverable and documented with deterministic examples that match implemented CLI behavior.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/cli/config-cli.test.ts` passes
- [ ] Runtime precedence between CLI flags/config/env is deterministic and covered by tests
- [ ] Help and README include active config command examples
</verification>

<success_criteria>

- All tasks completed
- CLI-02 behavior is fully implemented
- Config setup is agent-friendly and contract-tested

</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-release-gates/06-01-SUMMARY.md`
</output>
