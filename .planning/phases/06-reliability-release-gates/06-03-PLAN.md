---
phase: 06-reliability-release-gates
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - test/fixtures/output/markdown-golden.md
  - test/fixtures/output/json-golden.json
  - test/output/golden-fixture.test.ts
  - scripts/verify-fixtures.ts
  - package.json
  - README.md
autonomous: true
requirements: [ERR-04]
user_setup: []
must_haves:
  truths:
    - "At least one golden output fixture exists and is version-controlled"
    - "Fixture verification uses deterministic mock inputs and fixed timestamps"
    - "Fixture drift is detected automatically by tests and scriptable checks"
  artifacts:
    - "test/fixtures/output/markdown-golden.md"
    - "test/fixtures/output/json-golden.json"
    - "test/output/golden-fixture.test.ts"
    - "scripts/verify-fixtures.ts"
  key_links:
    - "Golden fixtures in `test/fixtures/output/*` are validated by `test/output/golden-fixture.test.ts`"
    - "`scripts/verify-fixtures.ts` is reusable by local workflows and CI to enforce fixture parity"
---

<objective>
Add golden fixtures and deterministic contract-drift protection.

Purpose: satisfy ERR-04 with explicit, automated fixture-based regression gates.
Output: canonical golden fixtures + fixture validation tests + verification script.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reliability-release-gates/06-RESEARCH.md
@.planning/phases/06-reliability-release-gates/06-02-PLAN.md
@src/output/build-artifact.ts
@src/output/markdown.ts
@src/output/json.ts
@test/output/output-parity.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deterministic golden fixtures from fixed canonical input</name>
  <files>test/fixtures/output/markdown-golden.md, test/fixtures/output/json-golden.json</files>
  <action>Generate at least one Markdown fixture and one JSON fixture from the same fixed canonical artifact input (including fixed timestamp) so fixtures are deterministic and reproducible. Document fixture provenance in file header/comments if applicable.</action>
  <verify><automated>bun test test/output/golden-fixture.test.ts</automated></verify>
  <done>Fixture files are committed and reproducible from deterministic canonical input assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture regression tests guarding schema/format drift</name>
  <files>test/output/golden-fixture.test.ts, test/output/output-parity.test.ts</files>
  <action>Add tests that compare current renderer outputs against golden fixtures and fail with actionable diff context when drift occurs. Ensure tests assert both content stability and semantic parity assumptions.</action>
  <verify><automated>bun test test/output/golden-fixture.test.ts test/output/output-parity.test.ts</automated></verify>
  <done>Fixture regressions fail predictably when markdown/json contracts drift from expected outputs.</done>
</task>

<task type="auto">
  <name>Task 3: Add reusable fixture verification script and docs hook</name>
  <files>scripts/verify-fixtures.ts, package.json, README.md</files>
  <action>Create a lightweight script command (for example `bun run verify:fixtures`) that executes fixture validation checks for local and CI usage. Document when to run it and expected failure behavior.</action>
  <verify><automated>bun run verify:fixtures</automated></verify>
  <done>Fixture verification is scriptable and documented for release workflows.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/output/golden-fixture.test.ts` passes
- [ ] Golden fixtures are deterministic and committed
- [ ] `bun run verify:fixtures` runs successfully
</verification>

<success_criteria>

- All tasks completed
- ERR-04 is enforced by fixture-based regressions
- Fixture verification can run both locally and in CI

</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-release-gates/06-03-SUMMARY.md`
</output>
