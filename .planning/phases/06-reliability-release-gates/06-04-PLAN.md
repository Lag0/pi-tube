---
phase: 06-reliability-release-gates
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - .github/workflows/ci.yml
  - package.json
  - README.md
  - docs/release-checklist.md
  - test/cli/install-flow.test.ts
autonomous: true
requirements: [ERR-02, ERR-04]
user_setup: []
must_haves:
  truths:
    - "CI pipeline enforces deterministic test + fixture gates on changes"
    - "Release checklist documents mandatory reliability checks before tagging"
    - "Local scripts and CI gates use the same verification commands"
  artifacts:
    - ".github/workflows/ci.yml"
    - "docs/release-checklist.md"
    - "package.json"
  key_links:
    - "CI workflow in `.github/workflows/ci.yml` executes `bun test` and fixture verification script from `package.json`"
    - "Release checklist references the same commands enforced by CI to avoid drift"
---

<objective>
Implement CI quality gates and release checklist for reliability hardening.

Purpose: finalize phase reliability guarantees by making regression gates mandatory in automation.
Output: CI workflow + release checklist + docs/tests alignment.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reliability-release-gates/06-RESEARCH.md
@.planning/phases/06-reliability-release-gates/06-03-PLAN.md
@package.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic CI workflow for tests and fixture verification</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <action>Create a CI workflow that installs Bun dependencies and runs deterministic gate commands (`bun test`, fixture verification script). Keep workflow minimal and stable for pull requests and default branch pushes.</action>
  <verify><automated>bun test</automated></verify>
  <done>CI workflow is committed and references deterministic local verification commands.</done>
</task>

<task type="auto">
  <name>Task 2: Add release checklist covering reliability gates</name>
  <files>docs/release-checklist.md, README.md</files>
  <action>Document a release checklist with mandatory checks: full test suite, fixture verification, command-contract smoke runs, and error contract sanity checks. Ensure checklist is concise and executable.</action>
  <verify><automated>bun test test/cli/install-flow.test.ts test/cli/help.test.ts</automated></verify>
  <done>Release checklist exists and is referenced from project docs with actionable commands.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for CI/documented gates</name>
  <files>test/cli/install-flow.test.ts, README.md, package.json</files>
  <action>Extend contract tests to assert CI/release-gate references exist and remain aligned with actual scripts/commands. Prevent silent drift between docs, scripts, and automation workflow.</action>
  <verify><automated>bun test test/cli/install-flow.test.ts</automated></verify>
  <done>CI and release-gate documentation are test-covered and resistant to accidental drift.</done>
</task>

</tasks>

<verification>
- [ ] CI workflow file exists and references deterministic gate commands
- [ ] `docs/release-checklist.md` exists with executable release steps
- [ ] CI/docs gate references are regression-tested
</verification>

<success_criteria>

- All tasks completed
- Reliability checks are enforced in automation and documented for release
- Phase 6 is ready for verification and milestone completion

</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-release-gates/06-04-SUMMARY.md`
</output>
