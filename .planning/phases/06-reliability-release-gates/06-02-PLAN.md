---
phase: 06-reliability-release-gates
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/errors/catalog.ts
  - src/errors/cli-errors.ts
  - src/cli/build-cli.ts
  - src/cli/handlers.ts
  - src/transcription/providers/deepgram.ts
  - src/transcription/providers/groq.ts
  - test/errors/error-taxonomy.test.ts
  - test/cli/error-exit-codes.test.ts
  - test/cli/transcription-cli.test.ts
autonomous: true
requirements: [ERR-01, ERR-02, ERR-03]
user_setup: []
must_haves:
  truths:
    - "Error codes are centrally defined and reused instead of ad-hoc string construction"
    - "Failure paths return deterministic non-zero exits while success paths remain zero"
    - "Error guidance stays concise and consistent for agent/human remediation"
  artifacts:
    - "src/errors/catalog.ts"
    - "src/errors/cli-errors.ts"
    - "test/errors/error-taxonomy.test.ts"
    - "test/cli/error-exit-codes.test.ts"
  key_links:
    - "`src/errors/catalog.ts` provides canonical code metadata consumed by constructors in `src/errors/cli-errors.ts`"
    - "CLI execution in `src/cli/build-cli.ts` maps all thrown `CliError` instances through `formatCliError()` for stable exit contracts"
---

<objective>
Implement centralized error taxonomy and exit policy hardening.

Purpose: satisfy ERR-01, ERR-02, and ERR-03 with one enforceable source of truth.
Output: error catalog + constructor alignment + exit/guidance regressions.
</objective>

<execution_context>
@/Users/brunolago/.codex/get-shit-done/workflows/execute-plan.md
@/Users/brunolago/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reliability-release-gates/06-RESEARCH.md
@.planning/phases/06-reliability-release-gates/06-01-PLAN.md
@src/errors/cli-errors.ts
@src/cli/build-cli.ts
@src/cli/handlers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical error-code catalog and align constructors</name>
  <files>src/errors/catalog.ts, src/errors/cli-errors.ts, test/errors/error-taxonomy.test.ts</files>
  <action>Create a centralized public error-code catalog (code, default exit, guidance expectations) and refactor existing CLI/transcription/intake errors to consume it. Ensure no behavior regression for already-public codes while making future additions deterministic.</action>
  <verify><automated>bun test test/errors/error-taxonomy.test.ts</automated></verify>
  <done>Error constructors map to a single canonical taxonomy and tests fail on catalog/constructor drift.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce deterministic exit policy and guidance formatting</name>
  <files>src/cli/build-cli.ts, src/errors/cli-errors.ts, test/cli/error-exit-codes.test.ts</files>
  <action>Harden CLI error handling so all known failure classes return deterministic non-zero exits and concise guidance lines, with stable formatting for parser safety. Ensure unexpected failures remain clearly distinguishable from known contract errors.</action>
  <verify><automated>bun test test/cli/error-exit-codes.test.ts</automated></verify>
  <done>Exit policy and guidance formatting are deterministic and regression-tested across key failure paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration regressions for taxonomy coverage in runtime flows</name>
  <files>src/cli/handlers.ts, src/transcription/providers/deepgram.ts, src/transcription/providers/groq.ts, test/cli/transcription-cli.test.ts, test/cli/intake-cli.test.ts</files>
  <action>Add/refresh integration tests to assert that real runtime failures (source intake errors, provider auth/rate-limit/unavailable failures, contract violations) map to catalog-backed codes and expected exits. Keep remediation guidance concise and deterministic.</action>
  <verify><automated>bun test test/cli/error-exit-codes.test.ts test/cli/transcription-cli.test.ts test/cli/intake-cli.test.ts</automated></verify>
  <done>Runtime integration tests prove catalog-backed codes and non-zero exits are stable across representative failures.</done>
</task>

</tasks>

<verification>
- [ ] `bun test test/errors/error-taxonomy.test.ts` passes
- [ ] `bun test test/cli/error-exit-codes.test.ts` passes
- [ ] Integration failure paths map to stable error codes and non-zero exits
</verification>

<success_criteria>

- All tasks completed
- ERR-01, ERR-02, ERR-03 are implemented and regression-protected
- Error contract additions are centralized and maintainable

</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-release-gates/06-02-SUMMARY.md`
</output>
